# name: 'Auto Assign'
# on:
#   pull_request:
#   issue_comment:
#     types: [opened, ready_for_review,created, edited]

# jobs:
#   add-reviews:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: kentaro-m/auto-assign-action@v1.2.1
#         with:
#           configuration-path: '.github/auto_assign.yml' # Only needed if you use something other than .github/auto_assign.yml
# # name: Assign issue to contributor
# # on:
# #   pull_request:
# #   issue_comment:
# #     types:  [created, edited]
    
# # jobs:
# #   assign:
# #     if: ${{ github.event.comment.body }} == *.take1*
# #     runs-on: ubuntu-latest
# #     steps:
# #     - name: take the issue
# #       uses: bdougie/take-action@main
# #       env:
# #         GITHUB_TOKEN: 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
# #       with:
# #         message: Thanks for taking this issue! Let us know if you have any questions!
# #         trigger: .take